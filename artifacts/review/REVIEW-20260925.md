# macOS and workspace review — 2026-09-25

## Scope and source

Reviewed application checkout `257d679` and VRT submodule `51853ba29703f51aceb2cfefe5a12a65a8e1110f`. The working tree was clean at the start. This review changes documentation and records evidence; it does not upgrade dependencies or repair application behavior.

## Findings

| Finding | Evidence and impact | Disposition |
| --- | --- | --- |
| Fresh-clone instructions checked absent generated outputs | **Observed source:** `generated/` is ignored and `build-image.sh` invokes `--check`, not generation. Following the original README could fail before building. | README and macOS guide now generate before checking/building. |
| Mac and guest workflows were insufficiently separated | **Observed source:** Lima has no host mounts; Docker builds and lifecycle scripts must run in the guest. Two sequential log-follow commands block, and capture files initially remain in the guest. | Added explicit terminal labels, VM-name persistence, log terminals, capture copy-out and cleanup instructions. |
| Current C++ qualification regressed relative to historical report | **Observed execution:** fresh macOS configure/build succeeded; 10/12 CTest targets passed. `sdr_vrt_runtime_retention_tests` failed `query` at line 34; `sdr_processor_controller_tests` failed `epoch` at line 299. | Historical 12/12 and runtime claims retained as historical; current failure evidence linked below. Root causes are **Unresolved**. A successful build does not qualify the current runtime. |
| VRT image metadata does not identify current source | **Observed source:** Docker copies checked-out VRT headers; `VRT_REVISION` only supplies a label. It still names `dbe85d3`, while checkout is `51853ba`. | Dependency documentation now explains the mismatch. Enforcing accurate source/label alignment remains work before the next image qualification. No dependency revision was changed. |
| Capture success is not capture completeness | **Observed source:** `capture.sh` prints, rather than saves, SHA-256; uses fixed replacement filenames; does not fail on nonzero kernel drops. **Documented historical result:** 10,928 drops. | Corrected hash claim; documented single capture at a time, copy-before-repeat and inspection of metrics. No current capture-quality claim. |
| Recorder error surfaced during cleanup | **Observed execution:** Containerlab reported `recorder: packet socket reported a terminal error` while removing the lab. | Preserved in cleanup log. Timing/root cause and whether this predated teardown are **Unresolved**; cleanup still completed. |

## Verification performed

- Fresh `cmake -S . -B build/dev -G Ninja -DCMAKE_BUILD_TYPE=Debug`: PASS on Apple Clang 21.
- `cmake --build build/dev -j 4`: PASS.
- `ctest --test-dir build/dev --output-on-failure`: **10 PASS, 2 FAIL**, exit 8; [unaltered CTest log](ctest-macos-20260925.log).
- `python3 -m unittest discover -s tests -p 'test_*.py'`: **4/4 PASS**.
- `python3 scripts/generate_config.py --check`: PASS for existing local generated outputs.
- `shellcheck scripts/*.sh`: PASS.
- `python3 -m compileall -q scripts tests/test_generate_config.py`: PASS.
- Documentation shell blocks checked with `bash -n`; local Markdown links checked; `git diff --check`: PASS.
- Installed Lima help checked for start/name/timeout and copy syntax. Shell/copy examples were not executed to create a new environment.

**Not run:** new VM provisioning, fresh-clone end-to-end setup, image rebuild, integrated SDR deployment, capture, Intel Mac qualification, or Linux reproduction of the two current CTest failures. Cleanup of the existing lab is not runtime qualification of this checkout.

## Cleanup performed

The user explicitly requested Containerlab VM cleanup. Of 33 identified `clab-*` instances, 32 were already stopped and were not started or inspected internally. The one running instance, `clab-vrt-20260925-135412`, contained the eight-node `four-radio-sdr` lab and three exited project-build containers.

Destroyed that lab through its existing topology; removed the three inspected exited build containers; verified no remaining containers or lab management network in that guest; stopped that VM. All 33 instances are now stopped. VM disks and cached images are retained; this was not disk deletion or global pruning. Stored resources in the 32 previously stopped VMs were not inspected or removed.

Evidence: [cleanup transcript](cleanup-20260925.log), [post-cleanup VM inventory](vm-cleanup-20260925.json).

## Next work

1. Diagnose the two failing C++ tests against the current VRT APIs; determine whether the regression is in the integration, timing assumptions or upstream behavior before changing expectations.
2. Make the image's VRT source label match and verify the actual submodule source used at build time.
3. Rebuild and qualify the resulting image in a fresh dedicated Linux VM, including sustained controller transactions and coordinated start. Record actual source/image identities and clean up that VM.
4. Address capture drops and the remaining historical fault/resource tests before claiming those acceptance requirements passed.
